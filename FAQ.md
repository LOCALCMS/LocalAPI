# 연동 작업 FAQ (담당자 예상 질문)

> 🚧 **본 문서는 예시(샘플)입니다.**
> 현재 실제 **마이그레이션·운영 적용 전** 단계이며, 엔드포인트 경로·파라미터·정책(토큰 유효시간, 호출 한도 등)은 **확정되지 않았습니다.**
> 실제 연동 전 반드시 담당자 확인이 필요합니다.


LOCALCMS API 연동 작업을 받은 담당자가 **시작 전·진행 중 되묻게 되는 질문**과 그에 대한 안내입니다.
(질문 = 작업 담당자 / 답변 = 작업을 의뢰한 LocalCMS 측)

> ※ `(확인 필요)`, `(예: ...)` 로 표시된 부분은 의뢰 시점에 실제 값으로 채워 전달해 주세요.

## 목차

- [시작 전 확인 (발급 · 접속 · 환경)](#시작-전-확인-발급--접속--환경)
- [작업 범위](#작업-범위)
- [구현 방법 (인증 · 호출 · 에러)](#구현-방법-인증--호출--에러)
- [데이터 이해](#데이터-이해)
- [일정 · 커뮤니케이션](#일정--커뮤니케이션)

---

## 시작 전 확인 (발급 · 접속 · 환경)

**Q. 이용기관 식별번호(orgcode)와 KEY(uuid)는 어디서 받나요?**
A. LocalCMS 담당자가 발급합니다. 작업 시작 시 식별번호·KEY를 전달드립니다. KEY는 외부 노출 금지이며 환경변수로 관리해 주세요.

**Q. 접속 IP 등록은 어떻게 하나요?**
A. 본 API는 등록된 IP에서만 호출됩니다. 연동 서버의 **고정 공인 IP**를 알려주시면 화이트리스트에 등록합니다. IP가 정해지면 가장 먼저 전달해 주세요. (등록 담당: 확인 필요)

**Q. 테스트(연동) 환경이 있나요? 테스트용 계정·키를 받을 수 있나요?**
A. (확인 필요) 테스트 환경 제공 여부 및 테스트용 식별번호·키 발급은 의뢰 시 함께 안내합니다. 운영 데이터로 테스트가 어려우므로 가능하면 테스트 환경에서 먼저 검증해 주세요.

**Q. 운영/테스트 서버 주소(도메인)는 무엇인가요?**
A. (확인 필요) 호출 기본 URL을 전달드립니다. 모든 호출은 반드시 `https://` 로만 가능합니다.

---

## 작업 범위

**Q. 이번에 구현할 기능은 어디까지인가요?**
A. (의뢰 시 명시) 보통 아래 중 필요한 것만 구현합니다.
- 납부내역 조회 (`/api/lpaylist/` 또는 `/api/tpaylist/`)
- 납부자 등록 (`/api/lcustins/`)
어떤 것이 필요한지, 화면/배치 중 어디에 붙는지 의뢰 시 함께 알려드립니다.

**Q. 조회 결과를 어디에 저장/표시해야 하나요?**
A. (의뢰 시 명시) 예) 관리자 화면에 표로 표시 / DB에 적재 후 정산 / 단순 확인용 등. 저장이 필요하면 마스킹된 민감정보를 그대로 저장할지 정책을 함께 정합니다.

**Q. 실시간 호출인가요, 배치(주기 호출)인가요?**
A. (의뢰 시 명시) 예) 매일 1회 전일자 조회 배치 / 사용자 요청 시 실시간 조회 등. 호출 빈도에 따라 한도(rate limit)를 고려해야 합니다.

**Q. 어떤 인증 방식으로 구현하면 되나요?**
A. **TOKEN 방식 권장**입니다. (만료가 있어 더 안전) KEY 방식도 가능하나 영구 키라 노출 위험이 큽니다. 특별한 사유가 없으면 TOKEN 방식으로 구현해 주세요.

---

## 구현 방법 (인증 · 호출 · 에러)

**Q. 토큰은 매번 발급하나요? 캐싱해도 되나요?**
A. 토큰은 일정 시간(예: 1시간) 후 만료됩니다. 매 호출마다 발급하지 말고 **발급한 토큰을 만료 전까지 재사용**하고, `401` 이 오면 그때 재발급하도록 구현해 주세요.

**Q. 조회 파라미터는 바디인가요, 헤더인가요?**
A. 납부내역 조회는 조회 조건(`stdt`, `enddt`, `custcd`, `value1`, `value_type`, `sum_type`, `seltype`)을 **HTTP 헤더**로 보냅니다. 납부자 등록은 등록 정보를 **form-urlencoded 바디**로 보냅니다. (언어별 예제는 EXAMPLES.md 참고)

**Q. `value_type` / `sum_type` / `seltype` 는 어떻게 조합하나요?**
A.

| 파라미터 | 값 | 의미 | 필수 동반값 |
|----------|----|------|------------|
| value_type | 0 / 1 / 2 | 전체 / 납부자번호 / 키값1(휴대폰) | 1→custcd, 2→value1 |
| sum_type | 0 / 1 | 개별 / 합산 | - |
| seltype | 0 / 1 | 실출금일 / 청구일 | - |

**Q. 에러·재시도는 어떻게 처리하면 되나요?**
A. 응답 HTTP 코드로 분기해 주세요. `200` 정상, `401` 인증/토큰 만료(재발급 후 재시도), `403` 미등록 IP, `429` 한도 초과(간격 두고 재시도), `400` 파라미터 오류. 무한 재시도는 피하고 횟수 제한을 두세요.

**Q. 한글이 깨지는데요?**
A. 요청·응답은 UTF-8 기준입니다. 클라이언트 인코딩을 UTF-8로 맞춰주세요. (Java는 `StandardCharsets.UTF_8` 명시)

---

## 데이터 이해

**Q. 미납내역이 안 나옵니다. 버그인가요?**
A. 정상입니다. 미납내역은 제공하지 않습니다. 청구금액(`PaymentAmt`)과 실출금액(`PaidAmt`) 중 **하나만** 사용하세요.

**Q. 청구일과 실출금일이 다릅니다.**
A. 청구일이 주말·공휴일이면 다음 영업일에 출금됩니다. 어느 날짜 기준으로 조회할지는 `seltype` 으로 정합니다.

**Q. 계좌번호·주민(사업자)번호가 가려져 옵니다.**
A. 개인정보 보호를 위한 마스킹 처리입니다. 정상이며, 원본 값은 응답으로 제공되지 않습니다.

**Q. 납부자번호(custcd)를 비워서 등록하라는데 맞나요?**
A. 자동채번 기관은 공란으로 두면 번호가 자동 부여됩니다. 부여된 번호는 등록 응답/조회로 확인합니다.

**Q. 조회 가능한 기간 범위가 있나요?**
A. 오늘 기준 직전년도 01월 01일부터 조회 가능합니다. 날짜 포맷은 `YYYYMMDD` 입니다.

---

## 일정 · 커뮤니케이션

**Q. 마감 일정은 언제인가요?**
A. (의뢰 시 명시)

**Q. 막히는 부분이 있을 때 누구에게 문의하나요?**
A. LocalCMS 담당자(연락처: 확인 필요 / 메일: localcms@naver.com)에게 문의해 주세요. 명세 외 동작이나 추가 엔드포인트(변경·해지 등) 필요 여부도 함께 확인합니다.

**Q. 산출물은 어떤 형태로 전달하나요?**
A. (의뢰 시 명시) 예) 연동 모듈 소스 + 호출 테스트 결과 + 간단한 사용 안내.
